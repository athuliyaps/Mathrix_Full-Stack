# Mathrix_Full-Stack


## Quick Setup
```bash
# Client (React)
git clone https://github.com/athuliyaps/Mathrix_client.git client/
cd client && npm i && npm start

# Server (Node.js)  
git clone https://github.com/athuliyaps/Mathrix_server.git server/
cd server && npm i && npm run dev
